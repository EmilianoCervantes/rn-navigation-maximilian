/**
 * Si seleccionamos una comida dentro de una categoría o
 * en el listado de favoritos.
 * Muestra los pasos (detalle) para preparar una comida.
 */
import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const DetalleComidaScreen = () => {
  return (
  <View>
  </View>
  );
};

const estilos = StyleSheet.create({});

export default DetalleComidaScreen;