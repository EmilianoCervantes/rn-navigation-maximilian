/**
 * Establecer filtros como:
 * vegano, vegatariano, gluten free, etc.
 */
import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const FiltroScreen = () => {
  return (
  <View>
  </View>
  );
};

const estilos = StyleSheet.create({});

export default FiltroScreen;