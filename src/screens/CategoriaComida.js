/**
 * Carga el listado dependiendo la categoría elegida.
 */
import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const CategoriaComidaScreen = () => {
  return (
  <View>
  </View>
  );
};

const estilos = StyleSheet.create({});

export default CategoriaComidaScreen;